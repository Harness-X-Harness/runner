CONSTANTS
  Sessions = {sessionA, sessionB}
  Clients = {clientA, clientB}
  Commands = {commandA, commandB}
  Generations = {generationA, generationB}
  InitialGeneration = generationA

SPECIFICATION CrossFailureSpec
CHECK_DEADLOCK FALSE

INVARIANT SessionFailureIsLocal
