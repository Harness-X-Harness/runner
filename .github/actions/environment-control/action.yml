name: Environment control-plane lifecycle
description: Claim the exact run and publish its ready private descriptor.
inputs:
  phase:
    description: Claim the run or publish the ready descriptor.
    required: true
  control-plane-url:
    description: Worker control-plane URL.
    required: true
  environment-id:
    description: Opaque Environment generation identifier.
    required: true
runs:
  using: node24
  main: index.js
