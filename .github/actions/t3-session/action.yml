name: T3 session
description: Start T3 Code for an empty user workspace through a Quick Tunnel.

runs:
  using: composite
  steps:
    - shell: bash
      run: |
        curl -fsSL https://pkg.cloudflare.com/cloudflare-main.gpg | \
          sudo tee /usr/share/keyrings/cloudflare-main.gpg > /dev/null
        echo 'deb [signed-by=/usr/share/keyrings/cloudflare-main.gpg] https://pkg.cloudflare.com/cloudflared noble main' | \
          sudo tee /etc/apt/sources.list.d/cloudflared.list > /dev/null
        sudo apt-get update -qq
        sudo apt-get install -y -qq cloudflared

    - shell: bash
      run: |
        install -d -m 0700 "$HOME/workspace" "$HOME/private-runner-session/t3code"
        npx --yes t3@latest serve --host 127.0.0.1 --port 3773 "$HOME/workspace" \
          > "$RUNNER_TEMP/t3code.log" 2>&1 &
        cloudflared tunnel --url http://127.0.0.1:3773 \
          > "$RUNNER_TEMP/cloudflared.log" 2>&1 &

    - uses: ./.github/actions/await-log
      with:
        path: ${{ runner.temp }}/cloudflared.log
        pattern: '(https://[-a-z0-9]+[.]trycloudflare[.]com)'
        destination: ${{ runner.temp }}/t3-url

    - shell: bash
      run: |
        backend_url="$(cat "$RUNNER_TEMP/t3-url")"
        pairing_url="$(
          npx --yes t3@latest auth pairing create \
            --base-url "$backend_url" \
            --ttl 6h \
            --json | jq -er .pairUrl
        )"
        umask 077
        printf '%s\n' "$backend_url" > "$HOME/private-runner-session/t3code/t3-url"
        printf '%s\n' "$pairing_url" > "$HOME/private-runner-session/t3code/pairing-url"
        printf 'T3_URL=%s\nPAIRING_URL=%s\n' "$backend_url" "$pairing_url" \
          > "$HOME/private-runner-session/t3code/connection.txt"
        chmod 600 "$HOME/private-runner-session/t3code/t3-url" \
          "$HOME/private-runner-session/t3code/pairing-url" \
          "$HOME/private-runner-session/t3code/connection.txt"
