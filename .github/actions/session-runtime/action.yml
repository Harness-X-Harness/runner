name: Agent Session runtime
description: Connect the admitted Environment to its private Session control channel.
inputs:
  control-plane-url:
    description: Worker control-plane URL.
    required: true
  environment-id:
    description: Opaque Environment generation identifier.
    required: true
runs:
  using: node24
  main: index.js

