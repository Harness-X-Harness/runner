name: Development tools
description: Install the native Codex and Grok Build CLIs.

runs:
  using: composite
  steps:
    - shell: bash
      run: |
        curl -fsSL https://chatgpt.com/codex/install.sh | sh
        curl -fsSL https://x.ai/cli/install.sh | bash
